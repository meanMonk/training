import { TextDesignPipe } from './text-design.pipe';

describe('TextDesignPipe', () => {
  it('create an instance', () => {
    const pipe = new TextDesignPipe();
    expect(pipe).toBeTruthy();
  });
});
