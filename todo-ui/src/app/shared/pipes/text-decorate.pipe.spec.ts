import { TextDecoratePipe } from './text-decorate.pipe';

describe('TextDecoratePipe', () => {
  it('create an instance', () => {
    const pipe = new TextDecoratePipe();
    expect(pipe).toBeTruthy();
  });
});
