/**
 * - DOCOUMENT OBJECT MODEL
 * Directives :
 *  - Directive is function in angular which helps us to manipulate the DOM.
 *  1. Structral directive :
 *      -  function use to manipulate the DOM elements whenever the directive found in html
 *      a. *ngIf
 *      b. *ngFor
 *      c. *ngSwitch
 *      c. *ngComponentOutlet
 *      d. *ngSwitchDefault
 *  2. Attribute directives :
 *      - just to change style and appearance and behaviour of an element in DOM
 *      a. [ngClass]
 *      b. [ngStyle]
*/

/**
 * Task :
 *  use the hover / leave to make the change on style in attribute directive.
 *
*/